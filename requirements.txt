# Our own deps
google-genai>=0.3
python-dotenv>=1.0
requests>=2.31
pillow>=10.0

# Minimal subset of the ARC-AGI-3-Agents framework's deps that we actually need
# (we bypass the framework's __init__.py to avoid pulling langchain, smolagents,
# openai, langgraph etc.)
arc-agi>=0.9.1
pydantic>=2.11
numpy>=2.0

# --- Optional, used by the report/figures pipeline only ----------------------
# Uncomment if you want to render the figures with matplotlib / rich and you are
# installing via pip. uv users should prefer `uv sync --group dev` instead.
# matplotlib>=3.8
# rich>=13.0
